
{% extends 'base_authen.html.twig' %}
{% block body %}
       
            <div class="authentication">
                <!-- Container start -->
                        <div class="container">            
                            <form action="{{ path('msg-forgotten-password') }}" method="POST">
                                <div class="row justify-content-md-center">
                                    <div class="col-xl-5 col-lg-5 col-md-6 col-sm-12">
                                        <div class="login-screen">
                                            <div class="login-box">  
                                                
                                                <h4 style="text-align:center;color:#225de4">Problèmes de connexion ?</h4>
                                                <h5 style="text-align:center">Entrez votre votre adresse e-mail et nous vous enverrons un lien pour récupérer votre compte..</h5>
                                                
                                                <div class="form-group">
                                                    <input type="text" pattern="^([a-zA-Z0-9_\-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([a-zA-Z0-9\-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,3})(\]?)$"
                                                    class="form-control" placeholder="Enter votre adresse email..." name="_username" required/>
                                                </div>
                                                <div style="text-align:center">
                                                    <button type="submit" class="btn btn-primary">Trouver le compte</button>
                                                </div>
                                                <table width="100%">
                                                    <tr>
                                                    <td><hr /></td>
                                                    <td style="width:1px; padding: 0 10px; white-space: nowrap;">ou</td>
                                                    <td><hr /></td>
                                                    </tr>
                                                </table>
                                                <div style="text-align:center">
                                                <a href="{{ path('security_registration') }}"><b>Créer un compte</b></a>
                                                </div>
                                                <hr>
                                                <div style="text-align:center;padding-top: 50px;">
                                                    <a href="{{ path('security_login') }}"><b>Revenir à l’écran de connexion</b></a>
                                                </div>
                                                
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </form>   
                        </div>
                <!-- Container end -->
            </div>

{% endblock %}






















